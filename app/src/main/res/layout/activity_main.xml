<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.maksg.mobilebutler.MainActivity">

    <GridLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:columnCount="4"
            android:rowCount="2"
            android:orientation="horizontal"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true">

        <Button
                android:text="Запланировать событие"
                android:layout_gravity="fill"
                android:layout_columnSpan="2"
                android:layout_width="160dp"
                android:id="@+id/scheduleEventButton"
                android:onClick="onScheduleButtonClick"/>

        <Button
                android:text="Запланировать задачу"
                android:layout_gravity="fill"
                android:layout_columnSpan="2"
                android:layout_width="160dp"
                android:id="@+id/scheduleTaskButton"
                android:onClick="onTaskButtonClick"/>

        <Button
                android:text="Текущий профиль"
                android:layout_gravity="fill"
                android:layout_columnSpan="2"
                android:id="@+id/currentProfileButton"
                android:layout_width="160dp"
                android:onClick="onCurrentProfileButtonClick"/>

        <Button
                android:text="Конфигуратор профилей"
                android:layout_gravity="fill"
                android:layout_columnSpan="2"
                android:layout_width="160dp"
                android:id="@+id/configureProfileButton"
                android:onClick="onConfigureProfileButtonClick"/>
    </GridLayout>
</RelativeLayout>
